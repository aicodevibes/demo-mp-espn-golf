# Ticket 03: Tracked Player Hero Grid Theme Update

Type: prototype
Status: resolved
Blocked by: 01

## Question

How should `src/components/TrackedPlayerHeroGrid.tsx` be updated using `surface-container-lowest` card containers, `tertiary` (`#006d3a`) under-par score badges, `error` (`#ba1a1a`) over-par indicators, and `p-card` padding tokens?

## Answer

Updated `TrackedPlayerHeroGrid.tsx` to use `surface-container-lowest` (`#ffffff`) card backgrounds, `p-card` padding, `tertiary` (`#006d3a`) for under-par scores, `error` (`#ba1a1a`) for over-par scores, and high-contrast outline borders.

