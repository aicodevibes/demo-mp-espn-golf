# 01 — Date Formatting & Playoff Winner Detection Helpers

**What to build:** Create `formatEventDates(startDate, endDate)` to format ISO dates into human-readable ranges (e.g. "Jul 30 – Aug 2, 2026") and `getWinnerStatus(comp, eventStatus, competitors)` to identify single champions, playoff victories, and playoff runner-ups.

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] `formatEventDates` formats date ranges accurately.
- [x] `getWinnerStatus` identifies solo champions and playoff winners (`🏆 Champion (Playoff)` vs `2nd (Playoff)`).
- [x] Vitest unit tests verify date formatting and playoff tie handling.

